import React from "react";

export default function Header({ title }) {
  return (
    <header className="App-header">
      <div>
        <h1> 🧱 {title}</h1>
      </div>
    </header>
  );
}

//rfc
